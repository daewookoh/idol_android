<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme with Splash Screen support -->
    <!-- NoActionBar 설정: 코드 레벨에서 ActionBar 제어 -->
    <style name="Theme.Idol_android" parent="Theme.SplashScreen">
        <item name="android:statusBarColor">@color/purple_500</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Splash screen configuration -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Idol_android.NoSplash</item>
    </style>

    <!-- Theme after splash screen - NoActionBar -->
    <style name="Theme.Idol_android.NoSplash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/purple_500</item>
    </style>

    <!-- Material Components BottomSheet styles (공통 리소스) -->
    <style name="Theme.MaterialComponents.Light.BottomSheetDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Widget.Design.BottomSheet.Modal" parent="">
        <item name="android:background">@drawable/bg_bottom_sheet_dialog_fragment</item>
        <item name="android:elevation">16dp</item>
    </style>
</resources>